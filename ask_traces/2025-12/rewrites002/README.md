one, two punch - first a requestless FIM to combine two methods with just a new method name before them.. got it... then  go to a consumer and AskRewrite to use thew new full_analysis instead!

NOTE: the 1-fim relied on a combo of YANKS + RAG matches

