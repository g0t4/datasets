just an absolute masterpiece in RAG + very minimal request!
 nails it... even includes old variable names with pointer to new DTO values, so I can inline into rest of code!

chef's kiss!

one issue - wrapping in ``` markdown, annoying but not the end of the world and I strip that client side anyways
