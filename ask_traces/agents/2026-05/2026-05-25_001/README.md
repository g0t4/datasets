what I love most about this trace... I had the agent go back and format the lua code using the lua formatter I use through Coc in neovim and it nailed it... which is not simple given that the buffer with the modified code files are not the same buffer the agent runs inside of... so darn cool... I should consider renaming `run_lua` to convey that it is really `run_lua_in_neovim` or something like that... and add instructions about targeting buffers explicitly always and do not use bufnr=0 b/c of agent trace buffer ... and I should consider making the agent buffer less of a problem (i.e. close it when the `run_lua` tool executes?)
- I did have to tell the agent that I use Coc for my lua language server (not native neovim LSP)... but that is NBD...
