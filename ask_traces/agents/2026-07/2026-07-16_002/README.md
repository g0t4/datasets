qwen adding duplicate prefix detection to my FIM tooling and not only stripping it on accept (step1) but then flagging it in the streaming tokens with red extmarks (step2) then showing a green indicator with explanation on the end (step3) which I didn't ask for but that is cool too and then step4 was asking it to use extmarks over the actual prefix chars (original chars) that are duplicated so that is my cue when this kicks in so I can feel confident as i test this with models over time
