bang up job with Qwen saving me the time of writing a handful of abbrs for ansible-doc --list --type _
- and I asked for `adls_` in a vague way, I intended to be like `adlsi` but I like the `adlst_<plugin_type>` style cuz I can tab complete that!
- so I asked Qwen to port `adlsi` version to that style too...
- slight complaint that it did a mix of my style for the first one and then different pattern for all the rest... but it works at the end of the day
- my guess is it saw my notes about reminder abbrs in RAG matches and hence it went this direction
