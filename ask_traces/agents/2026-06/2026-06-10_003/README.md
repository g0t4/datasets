qwen's solution worked!
only complaint is it bailed after the first server port worked when I'd rather it try both! but I was able to comment out the `return` to test both!
actually I also don't like the `pip3 install requests --break-system-packages` that installs globally! yikes
 it should've stopped and asked me to what I wanted to do
praise - as always it tested its work, to confirm it runs!
