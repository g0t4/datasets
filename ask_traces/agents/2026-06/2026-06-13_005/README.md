works
accumulate and log reasoning content into the trace for fish shell + iterm ask-openai completions

complaint: name is a bit odd with `accumulated_reasoning_content` but I did say to accumulate it... however we also accumulate `content` and it is not named `accumulated_content`
  anyways a minor complaint
