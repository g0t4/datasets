bang up job! and yes I said passkey when I meant passphrase!
- love that Qwen knows the URL from "memory" alone :)
- TIL `--now` exists on systemctl enable/disable/etc commands! also starts/stops! so no need to call start after enable
- added a helpful reminder that AddKeysToAgent auto adds keys to ssh-agent instead of needing to `ssh-add path/to/kley`
- confirmed what I discovered on my own w.r.t. ssh-agent.service/socket, good to have a sanity check on that
