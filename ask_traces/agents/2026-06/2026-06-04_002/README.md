had Qwen adjust the MCP dot indicator to use a smaller dot character and use color to adjust the red/green instead of the fixed icon

did a good job with that

had to ask twice to "resume" (if you will)... before it made edits

Qwen appears to have gotten into a loop in reasoning, NBD... resume works

in this case, I would love to have a way to ask it to verify the change by automating neovim...
- TODO I should plugin a tool that captures screen, feeds it into locate anything model (nvidia) and lets model find things and query things about screen to see if it worked
