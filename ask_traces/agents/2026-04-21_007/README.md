capture trace of using langchain mcp server over HTTP streamble transport... it works!
- btw responses to filesystem tool are a bit odd (repeats alot)... that's the actual response (check my mcp-server-commands repo's stdio dir for an example of this tool call to see that's the original response and not my tooling messing up)
