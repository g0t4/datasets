gptoss120b is damn good at cycling values code! I've asked for several of these in different places (i.e. also in log level for ask-openai streamdeck button)...

ANYWAYS, this is EXACTLY what I wanted... so darn cool to have it now!!!

ALSO, love that gptoss120b fixed a bug in the code I had (`opts` => `self.opts`) too in `resize_width_ratio`
