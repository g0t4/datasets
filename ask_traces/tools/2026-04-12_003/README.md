Likes:
- it found code I already had to list directories and reused that (code I forgot about) - thanks to retrieval

setup caching of list of dir names in ~/.agent/skills dir so I can use for slash command completions in questions frontend
- a bit of trouble with ordering functions (local vs global)
- it did fix the issue once I pointed out that I like module level functions in this case (not global and not local)
