implement code for a failing lua test

btw while true feels icky but I have three conditions I search on for finding matches so there's really no way to avoid looping until no changes are made... and s/b fine unless I have a circular command reference... I can add detection for that too
