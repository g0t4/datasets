ask model to use `fd --help` and then map all short options into abbrs to expand to long options!
-  LOVE THIS!

slight frustration around sorting alphabetically... model did an ok job but some stuff was out of place...
- so I asked model to use a command to sort them for me
- it did that but then it mixed up order of captial vs lower on some options... i.e. -A => -a => -b => -B style mixup (NBD)
  ultimately I just resorted to using sort myself :)... works good

TODO if I do enough of these abbr expansions from short => long option... it might help to have a /SKILL for it
- can give explicit instructions
- can provide desired sort command too (as an asset the model can run)
