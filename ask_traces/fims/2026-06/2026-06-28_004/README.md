recently I've noticed gptoss does a really good job with FIM w/o repeating prefix characters (chars before cursor) when it comes to FIMs around:
`local log = require("config.logs").hammerspoons()`

interesting to note...  would be interesting to know why this line tends to not get messed up as often?

like here I am mid re[]quire and no issues... also noticed no issues when `local log = `
