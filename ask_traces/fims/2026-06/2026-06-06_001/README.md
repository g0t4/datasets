when porting my run_ abbr builder function to my dotfiles (from .config.fish in tree-sitter-qwen_chatml repo)... I let FIM take over and suggest a new impl with just the function name alone...
- THIS is also a great way to impl these... to extract the value too and bypass `npm run` entirely and run directly whatever it is...
  compelling to use this instead of `run_*` => `npm run *`
