correct FIM suggestion BUT it duplicates the current line's prefix which isn't needed
- likely b/c I am using an Qwen3.6 instruct model (not base model like Qwen/Qwen2.5-Coder-7B which is a base model, aka NOT instruct)
  NBD just a heads up I suspect this is due to using an instruct model (instead of base)
  TODO test Qwen3.5-35B-A3B-Base (currently no 3.6 Base models released :( )
