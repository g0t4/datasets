stellar FIM!
great example of FIM query/context approach for triggering a good RAG match, in this case `local foo` would not have worked the same as `local lua_vm_host` does
- I use the prefix of the current line (cursor line) when a prefix is available
