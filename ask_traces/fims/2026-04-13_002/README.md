this is the perfect FIM example
- I just added a list of new options in a separate mapping table and FIM mirrored them into this mapping table!
- only complaint is that it did not put a comment to mark the group of new options as related :)

## 1776109590-thread.json

I added `--` in a new line above the new options => and it added the header :)
- complaint => did not mirror same header's text in other mapping table
