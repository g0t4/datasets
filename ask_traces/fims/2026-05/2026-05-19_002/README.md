perfect FIM suggestion... only issue is duplicating the opening `(` but that is not a huge deal
- I've never been able to get gptoss to pay close attention to the existing code around the cursor...
  I get lucky plenty often it feels like...
  TODO I should add a second pass that looks at the suggestion and prompts for a fix if it duplicates!
  it could even look at validating the code is correct vs not... i.e. compiles!
