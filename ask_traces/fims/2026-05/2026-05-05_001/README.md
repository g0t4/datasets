FIM+RAG FTW ❤️

one small complaint => it should be `win.` and not `win:`
- but that's a small detail

I haven't used this API for getting the tool window in ages and I didn't even recall how to do it... love that FIM nailed it (basically) and I can fix the tiny mistake of `:` instead of `.`

oh and also complaint about spacing... but that's not new and it's a minor detail because tooling should honestly handle spacing (indent) discrepancies...
- in fact, I should consider a second pass of another LLM to fix the spacing (in paralel w/ a formatter run) and whatever finishes first could adjust the completion before I probably would get to it
- like using safety models where they block after completion starts generation, that way you don't slow down responses but you also can react when something is detected later on that would help to adjust automatically
