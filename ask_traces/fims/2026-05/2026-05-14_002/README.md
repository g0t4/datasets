FIM ❤️

## EVALS

these short=>long option examples:
- test model
    - knowledge of ripgrep (important as I would prefer models that can use `rg` over `ls`)
        - TODO setup similar for other commands like `fd`
    - knowledge of fish shell's `abbr`
    - especially when no prior examples in prefix (or RAG matches)
        - TODO simulate each mapping as a first mapping too (when building evals)
- following a simple pattern (when prefix has completed mappings)

- TODO compile these into executable test/eval cases

## 1778780563-trace.json

capturing FIMs for abbrs for ripgrep (rg) to map short=>long options...
   so if I type `-F` it expands to `--fixed-strings`:

## 1778780773-trace.json

this FIM is from typing up this README.m file with my FIM reactions/comments for datasets repo!
- I didn't even give an example of the short=>long and FIM (for this readme) got it right


## 1778781625-trace.json (-i/--ignore-case)

ok this SHOULD NOT have suggested -i/--ignore-case b/c I have the comment above saying to leave that alone!
- that said, this is the correct mapping

## 1778781528-trace.json

completed comment w.r.t. -i/--ignore-case and suggested correct closing parenthesis

## 1778781511-trace.json

multiple mappings (entire lines)
