notes: |
    stellar complete here! requires a deep understanding of git revision specifiers + deep understanding of what abbrs do in fish shell + that I want to swap reflog1 with @{1} b/c @{num} is a reflog specifier!

eval:
    - this would make for a good test of how knowledgeable a model is (technically speaking)
    - suggest redoing with `echo -s` (add `-s`) before triggering FIM completion
        - b/c without `-s` echo will add a space between the number and the FIM suggested "}" (curly brace)
            - thus `reflog1` turns into `@{1 }`
            - and with `-s` then `reflog1` turns into `@{1}`
            - `-s` means no spaces between args to echo
        - that said, leaving out the `-s` as a "mistake" might be a good test too (separate eval maybe?)
