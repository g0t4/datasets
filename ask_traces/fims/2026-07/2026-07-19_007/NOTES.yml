good: |
    - I asked for a FIM but I was already past the point of using `not` before the condition
    - so while it may seem like a bad FIM to suggest the entire line again... this is actually kinda good
      - bad: gptoss should've skipped the leading `if` which is now duplicated
      - good: but suggesting `not` even though already past that point is useful, signals mistake in cursor line prefix (or basically signals need for an Edit Prediction, not just FIM)
