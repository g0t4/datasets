notes:
    - great suggestion
complaints:
    - should've also suggested `)` closing parens for the regex either-or
evals:
    - highly technical eval b/c needs to know fish abbrs deeply + regex + know I wanna implement two abbrs with one function (an OR regex condition)
    - reminds me that I will want some evals to have partial credit if the model gives part of the completion but not all of it as is the case here with gptoss not including `)` on end
    - not "highly technical" but darn near close to it!
