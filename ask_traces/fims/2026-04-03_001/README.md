logic to split out a client module (gluing the pieces back together - both sides)
- i.e. adding module definition (local M = {}) and requires and then using the new client module in the old code
- nothing fancy, still awesome use for AI ... tedium

NOTES:
- btw 1775201392-thread.json => got the require for logging wrong, but was fixed by a later FIM with correct require call
