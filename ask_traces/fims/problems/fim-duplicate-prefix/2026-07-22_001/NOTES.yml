problems: |
    duplicated prefix, in this case not just indent
    cursorline prefix: `abbr ptk `  (including trailing space before cursor)
    suggestion: `abbr ptk 'pytest -k'`

good: otherwise this was the exact completion I was looking for as far as the `'pytest -k'` part
