## 1777614888-trace.json

good suggestion, but... the traceback is a string so it should not be pretty printed but rather displayed as-is using `add_no_markup(traceback)`
- still an argument to be made to pretty print it for a compact presentation, especially if logging many exceptions and regularly... could easily dominate the output

## 1777670085-trace.json

good rewrite!
