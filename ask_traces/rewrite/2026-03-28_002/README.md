turn my abbr into a reusable function and then use it to define other examples!

complaint... it didn't formulate parameters the way I asked (filter=foo) ... which would then require splitting on = ... but I can have it do that later when I move on to help for other types
