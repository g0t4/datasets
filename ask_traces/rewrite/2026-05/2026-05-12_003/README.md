nice rewrite! took my rich print scheme and ported it to use Tree for hierarchy

I like:
- renamed `tree` to `calls_tree` which is now a "subtree" of `root` so naming it meaningfully helps avoid ambiguity in which tree is which!

complaints
- removed one of my testing comments...
  - to be fair... I do have this guidance: "ABSOLUTELY NO trivial comments."
  - but I also have this: "When rewriting code, leave unrelated code and unrelated comments as is."
  - TODO I need to remove comment guidance in my instructions entirely and see what happens

