minor_complaint:
    - completion is 100% valid (works), but it is overly verbose
    - instead of `vim.fn.fnamemodify(vim.fn.expand('%:p'), ':.')`
      - simplify with `vim.fn.expand('%:p:.')`
      - or better yet `vim.fn.expand('%:.')`
    - but, to be fair, I did not ask for the most concise solution, I could've said that
      in fact, I'll follow up with a simplify rewrite (watch for that trace too)
