ok mixed feelings!

qwen correctly wrote the code for commit_ref (empty vs HEAD~$num")...
BUT WTF QWEN... WTF IS THE TRAILING if test -z $num return bullshit?! that does fucking nothing?

WTF MAN


OK FAIR ENOUGH, WES... WES... Qwen did the right thing actually...

it has no idea if there is code afterward that shouldn't run if num is empty!! ok fair, b/c I didn't give context it is RIGHT
-  MY DUMBASS IS WRONG!


TODO lets give the context going forward for rewrites OR lets add /context to do this conditionally (and keymap it <leader>rwc) so I can use both (standalone vs context)
