Good-ish solution for a rewrite... which only allows rewriting a seletion of code and there's not even context (not always) showing surrounding code
... given new code is inserted over old code, this solution is fine! and works when moved down outside of the dict/map table M.keys

TODO why don't I provide the full context too? and still just ask for the rewrite while also showing more of the file around the selection?

other observations:
- Qwen religiously follows my guidance about base0/base1 for 0/1 indexing! a bit over zealous, but I asked for it!
