testing a qwen fine tune: empero-ai/Qwythos-9B-Claude-Mythos-5-1M-GGUF

faster in terms of less thinking than my experience with qwen3.6-35b-a3b
- are the mythos traces geared toward less thinking?

ok but another issue...
- net result of change would be:
  `git --cached diff`
  when it has to be:
  `git diff --cached`
  because `--cached` is an option to the `git-diff` subcommand, not a `git` top level option
  perhaps this fine tune has trouble with math?

## Evals

This would make for a good eval!
- challenges model with math (if it goes insert route)
- and knowledge of git + git subommands!
- and is a pretty simple challenge
- definitely can be compiled/run as standalone lua code
