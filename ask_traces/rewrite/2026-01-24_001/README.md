I can't always recall in every language how to repeat a string (char)... super easy way to fix that :)

```ts
'='.repeat(20)
```

also little things like it counted exact number of `=` on each side and put that into the repeat call!
