excellent rewrite to take `stdin_text` and show it in the bash command itself instead of as a separate panel...
only complaint is indent was off by one level so it ended up not executing b/c it was nested in the get_display_command function just above... NBD easy fix
