love using rewrite still! with gptoss!
- tons of time saved on little things like this when I am writing the code

only complaint is that gptoss put the type annotation comments inside the function when they need to go right before the function (in lua)
