super cool refactoring of test assertions
- it got them all right... and, it was doing math to do so!
- look at how it incrementally goes after each match, checking for remaining after each match (or set) are migrated
  sed/grep/apply_patch
  interesting it didn't use semantic_grep, but hey, that's fine!
